services:
  - type: web
    name: hito-kyori-memo-mvp
    runtime: static
    buildCommand: npm install && npm run build:web
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
